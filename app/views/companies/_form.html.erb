<%= simple_form_for(@company) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :website %>
    <%= f.input :num_of_employees %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
